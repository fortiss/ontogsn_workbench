PREFIX gsn:  <https://w3id.org/OntoGSN/ontology#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX case: <https://w3id.org/OntoGSN/cases/ACT-FAST-robust-llm#>
PREFIX ex:   <https://example.org/car-demo#>

SELECT DISTINCT ?module ?label
WHERE {
  ?module gsn:contains ?seed ;
          a gsn:Module .
  OPTIONAL { ?module rdfs:label ?label }
}
ORDER BY COALESCE(?label, STR(?module))